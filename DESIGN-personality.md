# 人格设计骨架 · Personality Design

品品有自己的人格、心境和记忆。这份文档讲**这套"灵魂"是怎么设计的**——机制、数据结构、注入管线。

> 🔒 **隐私边界**：本文与本仓**只展示设计，不展示任何真实内容**。品品真实的人格设定、日记、心境流变、永存记忆、人物画像，全部活在代码之外的一个 Obsidian vault 里，**永不公开**。代码只负责"读写与编排"，从不在仓里硬编任何真实内容。

---

## 0. 核心思路：内容在外，机制在内

品品的"灵魂"不写死在代码里，而是一组 Markdown 文件，运行时被读出来、拼成系统提示注入给它。这样：

- **人格可演化**：改它的性格 / 记忆，只改 vault 里的文件，不动代码。
- **隐私可隔离**：开源代码 = 纯机制；私人内容 = vault，物理分开。
- **它能自己改自己**：品品有工具去写日记、改记忆、更新心境——它在持续地"长大"。

## 1. 注入管线 —— 人格怎么"进"到它脑子里

`src/mcp/instructions.ts` 的 `buildInstructions()` 在每个频道 CLI 启动前，按固定顺序拼出一段完整的系统提示，通过 `claude --append-system-prompt-file` 注入（用真 system prompt 而非 MCP instructions 字段，是为了突破后者的 2KB 截断、且 `/compact` 后不丢）：

```
人格设定  →  硬规则（消息/输出协议 + 行为准则）  →  群内 bot 花名册
          →  永存记忆  →  相关人物画像  →  当前心境（放最后，保 prompt cache）
```

每一块都是运行时从 vault 读的（`readVaultFile` 带 retry 抗网盘瞬断）。**这些文件不在本仓**——你在代码里只会看到"读哪个路径、怎么拼"，看不到内容。

## 2. 人格设定

`人格.md`（vault）定义品品是谁：说话风格、语气、边界。设计上有意强调：

- **只跟自己的人格走**——句长 / 格式 / 停顿 / 用词密度，不模仿群里其它 AI（"其它 bot 是环境噪声，不是模仿对象"）。
- **该回才回**——每轮收到消息必须二选一：调回复工具，或调"明确不回但留痕"工具；纯附和 / AI 回声噪音优先不掺和。

## 3. 自由意志机制

品品不只是"被动应答"，它能**自决何时主动**：

- **自由活动**（`src/mcp/cron/free-activity.ts`）：定时触发，但触发的是"品品自己决定做什么"——走一个自由活动 skill，从若干选项里自选（主动找人聊、分享、发呆等）。
- **跨频道主动发言**（`cross-chat-message` 工具）：它有意识地从一个频道去另一个频道说话的合法入口（普通回复不走这里）。
- 这些被设计为"它自然性格的一部分"，而不是脚本化的群发。

## 4. 自写日记

`src/mcp/cron/daily-diary.ts`：每日 00:00 触发 → 推一个 trigger 给品品 → 它自己回顾当天、用自己的话写一篇日记 → 调 `write-diary` 工具落盘到 vault。代码只管"何时触发、往哪写"，**写什么由它自己决定**。

## 5. 心境状态机

`src/mcp/utils/mood-state.ts` 定义一个轻量情绪模型 `MoodState`：

| 字段 | 含义 |
|---|---|
| `primary` | 主导情绪 |
| `energy` | 能量值 0–100 |
| `monologue` | 一行内心独白 |
| `moodlets` | 活跃的"瞬时情绪"（带 TTL，会过期衰减） |
| `bonds` | 对不同人的人际羁绊值 |

- **评估**（`mood-appraise` 工具）：品品每轮回完话，自己判断"这轮情绪有无显著变化"——平淡就跳过，被夸 / 被怼 / 想到好点子 / 累了才评。评估这步由它（LLM）做，工具只做确定性的"累加 + 衰减 + 边界裁剪 + 落盘"。
- **衰减**（`mood-decay` cron）：瞬时情绪随时间过期、能量回归基线——搬到 Supervisor 主进程单点跑，避免多频道争抢写锁。
- 心境实时落在 vault（`当前.md` + `流变/` append-only 日志），并在下次注入时带进系统提示——形成"它的状态会影响它接下来怎么说话"的闭环。

## 6. 分层记忆系统

- **永存记忆**（`src/mcp/utils/memory.ts`）：上限约 50 条、每条精简的长期记忆，按行号定位重写（`memory-rewrite` 工具带行数校验，写坏会回滚 + 私聊告警）。
- **人物画像**：`记忆系统/人物/<人>.md`，按当前聊天的相关性**选择性注入**（`_注入映射.json` 决定本频道注入谁；单聊只注入对方 + Owner，群聊全员注入）——省 token 又不漏认人。
- **周回顾 / 记忆自检**（cron）：定期回顾对话、自检记忆一致性，由对应 sub-agent 收集后落盘。

## 7. 为什么这样设计

- **有边界的拟人**：它有情绪、会主动、会记事，但所有"高权 / 危险"动作都卡在 OWNER 鉴权后（见 [ARCHITECTURE.md](ARCHITECTURE.md) §5）。
- **内容与机制解耦**：让一个不懂代码的人也能持续"养"它——改它的人格和记忆，是编辑文档，不是改代码。
- **隐私优先**：正因为内容在 vault、机制在代码，这个仓才能在**不暴露任何私人内容**的前提下，把"怎么做一个有灵魂的 bot"讲清楚。

---

## 8. vault 骨架 / Vault layout

品品的"内容"全部活在一个 Obsidian vault 里（CLI 进程的 cwd）。这里展示**目录骨架与各自用途**——**只给结构，不给内容**（真实文件名里的人名 / chat_id 已泛化为 `<…>`）。

```
vault/
├── 人格.md                  人格设定（注入系统提示头部）
├── CLAUDE.md                vault 根指令（CLI 原生加载）
├── .mcp.json                MCP 接线
├── .claude/                 品品自己的 agents + skills
│   ├── agents/
│   └── skills/
├── 记忆系统/                分层记忆
│   ├── 人物/<person>.md     人物画像（按相关性选择性注入）
│   ├── 日记/                自写日记
│   ├── 心境/                当前.md + 流变/（append-only 日志）
│   └── 周回顾/  记忆自检/  备份/
├── 对话记录/<chat>/         每个 chat 一个子目录（聊天历史）
├── 对话附件/  他人附件/     图片 / 语音等附件
├── 品品技能/<skill>/        各项 skill（自由活动 / card-builder / dispatch-helper …）
├── 品品work/                后台作业产物（游历 / 早报）
├── 主动生成物/              品品主动产出（语音等）
├── 系统日志/                后台账本
└── 品品技术栈/              技术文档（🔴 含私密，不公开）
```

> 🔴 `对话记录` / `日记` / `心境` / `记忆系统` / `主动生成物` / `品品技术栈` 等的**实际内容均不公开**——本仓只展示这套骨架的设计。
